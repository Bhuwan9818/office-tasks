# Copy these into Vercel → Project Settings → Environment Variables

DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/office_tasks"
SESSION_SECRET="generate-with-openssl-rand-hex-32-minimum-32-chars"
NODE_ENV="production"
